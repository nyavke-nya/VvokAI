# Исправления от 6 сентября 2026

База: 0ae1ccf. Предыдущие Python-исправления сохранены; пользовательский cfg/bot_config.toml вручную не изменялся.

## Сделано

- 08–09: сброс уворота очищает контекст, тактические callbacks, игрока, commitment solver и экстренную команду. Обработка кадра, сброс и публикация контекста сериализованы одним RLock; старый сдвиг не перезаписывает свежий контекст.
- 21: очередь JSON и история CSV публикуются через уникальный временный файл, flush/fsync и os.replace. Ошибка записи сохраняет предыдущий файл. Это не межпроцессная блокировка read–modify–write.
- 23–24: одна отправка телеметрии одновременно, пауза 5 минут после попытки; профиль истории вычисляется лениво в отправляющем потоке. Выключенная телеметрия его не вычисляет.
- 06, дополнительная защита: повреждённые metadata не роняют список; списки бойцов и режимов нормализуются. Временная загрузка очищается и при сбое file_storage.save.
- 05, частично: отвергается чужой Origin и cross-site команда браузера. В настройках маскируются API/Discord/Telegram токены, пароль и webhook URL; сохранение маски сохраняет прежнее значение.
- 30, частично: числовые настройки панели отвергают NaN, infinity, отрицательные значения, нулевые потоки/IPS/масштаб, неверные порты и confidence вне диапазона. Прямое редактирование TOML пока не проходит этот валидатор.
- Фронтенд: новая чёрно-белая композиция главной страницы, светлая панель сессии, текущий боец и цель, тёмный live view, метрики и очередь; общая монохромная тема остальных разделов, мобильная навигация, фокус клавиатуры, reduced motion. Убран скрипт декоративных эффектов.
- Исправлены повторное накопление document listeners расписания, дублирующийся ID перехода к очереди, отсутствие disabled у Start, неверное сообщение работающей сессии и переполнение заголовка длинной ошибкой API.
- Запускатель тестов использует UTF-8 для вывода и дочерних процессов, включая перенаправление в файл Windows.

## Проверка

Полный tests/run.py: 23 файла, 1108 успешных проверок, 0 ошибок. Новый test_audit_fixes.py содержит 24 поведенческие проверки: сбой и конкуренция записи, сброс и синхронизация уворота, валидация, секреты, телеметрия, Flask Origin. После заключительной нормализации metadata и auto эти 24 проверки повторно прошли. app.js и i18n.js прошли node --check. Визуально проверялись главная, каталог и плейстайлы, ширины 1280 и 390 px. Матчи не запускались.

Во время загрузки панели штатный механизм API сообщил о попытке перевыпуска ключа, затем о нестабильном внешнем IP. Синхронизация кубков не подтверждена. Flask-конструкторы в новых тестах затем изолированы заглушками, чтобы повторные тесты не запускали эту инициализацию.

## Остаётся открытым

Это продолжение исправлений, а не закрытие всех пунктов аудита. Не реализованы изоляция исполняемых плейстайлов (04), полноценный вход/Host-защита для LAN-панели (05), транзакция и координация обновления между процессами (13–14), защита чужих ключей developer account (15), подтверждение реально выбранного бойца (16), идентификатор матча для дедупликации результата (18), межпроцессные read–modify–write блокировки конфигов (20), подключение фактической тактики к runtime (25), согласование установщиков и сборки (26–27). Эти пункты требуют отдельных изменений и проверок; маскировка секретов не заменяет авторизацию, AST-фильтр не является песочницей.

RLock удерживается на время обработки кадра уворота. Корректность гонки проверена, но задержку на реальном устройстве следует измерить до утверждения о сохранении производительности.
