aiohttp~=3.13
numpy~=2.3
onnxruntime-directml~=1.24
requests~=2.32
toml~=0.10
torch~=2.8
torchvision~=0.23
pillow>=11.2.1
discord.py~=2.7
packaging>=25.0
pywin32>=311
easyocr~=1.7
adbutils~=2.12
av~=12.3
Flask~=3.1
pycryptodome~=3.21

# Last on purpose. easyocr depends on opencv-python-headless, and both packages
# unpack into the same cv2/ folder - so whichever pip installs last is the one
# that actually runs. When headless wins, every OpenCV GUI call raises "the
# function is not implemented" and the debug window can never open, with no
# error anywhere obvious. Listing it after easyocr makes the GUI build land
# last on a fresh install.
opencv-python~=4.11
